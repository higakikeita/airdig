# AirDig UI モックアップ

## SkyGraph ビューの見た目

### 全体レイアウト

```
┌──────────────────────────────────────────────────────────────────────┐
│  AirDig                                    🔍 Search   👤 User  ⚙️   │
├──────────────────────────────────────────────────────────────────────┤
│ [SkyGraph] [DeepDrift] [TraceCore] [PulseSight] [Unified View]      │
├────────────┬─────────────────────────────────────────────┬───────────┤
│            │                                             │           │
│  Filters   │          Graph Canvas                       │ Inspector │
│            │                                             │  Panel    │
│  ☐ VPC     │                                             │           │
│  ☑ Subnet  │         ┌─────────┐                        │ Selected: │
│  ☑ EC2     │         │  VPC-A  │                        │           │
│  ☑ RDS     │         │ 🟦      │                        │ EC2       │
│  ☐ Lambda  │         └────┬────┘                        │ i-abc123  │
│            │              │                              │           │
│  Regions   │         ┌────┴────┬────────┐               │ Type:     │
│  ☑ us-ea-1 │         │         │        │               │ t3.large  │
│  ☐ us-we-2 │    ┌────▼───┐┌───▼───┐┌──▼────┐          │           │
│            │    │Subnet-1││Subnet2││Subnet3│          │ Status:   │
│  Health    │    │  🟨    ││  🟩   ││  🟩   │          │ ● Running │
│  🟢 Healthy│    └────┬───┘└───┬───┘└───┬───┘          │           │
│  🟡 Warning│         │        │        │               │ Health:   │
│  🔴 Critical│    ┌───┴─┐  ┌──┴──┐ ┌──┴──┐            │ 🟡 Warning│
│            │    │EC2-1│  │EC2-2│ │EC2-3│            │           │
│            │    │ 🟩  │  │ 🟡  │ │ 🟩  │            │ Metrics:  │
│            │    └──┬──┘  └──┬──┘ └──┬──┘            │ CPU: 75%  │
│            │       │        │        │               │ Mem: 60%  │
│            │       └────────┴────────┘               │           │
│            │              │                          │ Drift:    │
│            │         ┌────▼────┐                     │ 2 events  │
│            │         │  RDS-1  │                     │ • SG mod  │
│            │         │   🟩    │                     │ • Type ch │
│            │         └─────────┘                     │           │
│            │                                         │ [Details] │
│            │                                         │           │
└────────────┴─────────────────────────────────────────┴───────────┘
│ Timeline: [========●===============] 2024-12-10 14:30            │
└──────────────────────────────────────────────────────────────────────┘
```

---

## 実際の見た目の詳細

### 1. グラフキャンバス（中央メイン部分）

#### ノードの表示

```
┌─────────────────────┐
│   VPC-production    │  ← リソース名
│   ┌─────────────┐   │
│   │     🟦      │   │  ← アイコン（リソースタイプ別）
│   │  10.0.0.0/16│   │  ← 主要情報
│   └─────────────┘   │
│   vpc-abc123        │  ← リソースID
└─────────────────────┘
```

**色の意味：**
- 🟢 **緑** = Healthy（正常）
- 🟡 **黄** = Warning（警告あり）
- 🔴 **赤** = Critical（重大な問題）
- ⚪ **灰** = Unknown（データなし）

**リソースタイプ別アイコン：**
- 🟦 VPC
- 🟨 Subnet
- 🟪 Security Group
- 🟩 EC2
- 🟧 RDS
- 🟫 Lambda
- ⬜ S3

#### エッジ（線）の表示

```
VPC ──────────────> Subnet   (実線 = ownership)
Subnet ············> EC2      (点線 = network)
EC2 - - - - - - - > RDS      (破線 = dependency)
```

**線の種類：**
- **実線（──）** = ownership（所有関係）
- **点線（··）** = network（ネットワーク接続）
- **破線（- -）** = dependency（依存関係）
- **赤線** = drift detected（ドリフト検出）
- **青線** = call（サービス呼び出し、TraceCore）

---

### 2. インスペクターパネル（右側）

ノードをクリックすると詳細が表示されます：

```
┌─────────────────────────────────────┐
│ EC2 Instance Details               │
├─────────────────────────────────────┤
│ ID: i-0abc123def456                │
│ Name: web-server-1                 │
│ Type: t3.large                     │
│ State: ● Running                   │
│                                    │
│ ┌─ Health Status ─────────────┐   │
│ │ Status: 🟡 Warning          │   │
│ │ Reason: High CPU usage      │   │
│ │                             │   │
│ │ CPU:    ████████░░ 75%      │   │
│ │ Memory: ██████░░░░ 60%      │   │
│ │ Network: ████░░░░░░ 40 Mbps │   │
│ └─────────────────────────────┘   │
│                                    │
│ ┌─ Drift Events ──────────────┐   │
│ │ 2 changes detected          │   │
│ │                             │   │
│ │ ⚠️ Security Group modified  │   │
│ │   5 minutes ago             │   │
│ │   by: john@example.com      │   │
│ │   [View Details]            │   │
│ │                             │   │
│ │ 📝 Instance type changed    │   │
│ │   2 hours ago               │   │
│ │   t3.medium → t3.large      │   │
│ │   [View Details]            │   │
│ └─────────────────────────────┘   │
│                                    │
│ ┌─ Connected Services ────────┐   │
│ │ • api (TraceCore)           │   │
│ │ • worker (TraceCore)        │   │
│ │ → rds-prod-01               │   │
│ └─────────────────────────────┘   │
│                                    │
│ ┌─ Tags ──────────────────────┐   │
│ │ Environment: production     │   │
│ │ Team: platform              │   │
│ │ CostCenter: eng-001         │   │
│ └─────────────────────────────┘   │
│                                    │
│ [View in AWS Console]             │
│ [View Metrics Dashboard]          │
└─────────────────────────────────────┘
```

---

### 3. フィルターパネル（左側）

```
┌──────────────────────────┐
│ Filters                 │
├──────────────────────────┤
│ Resource Types          │
│ ☑ VPC                   │
│ ☑ Subnet                │
│ ☑ Security Group        │
│ ☑ EC2                   │
│ ☑ RDS                   │
│ ☐ Lambda                │
│ ☐ S3                    │
│                         │
├──────────────────────────┤
│ Regions                 │
│ ☑ us-east-1             │
│ ☐ us-west-2             │
│ ☐ ap-northeast-1        │
│                         │
├──────────────────────────┤
│ Health Status           │
│ ☑ 🟢 Healthy (15)       │
│ ☑ 🟡 Warning (3)        │
│ ☑ 🔴 Critical (1)       │
│ ☐ ⚪ Unknown (0)        │
│                         │
├──────────────────────────┤
│ Drift Status            │
│ ☑ No Drift              │
│ ☑ Drift Detected (5)    │
│                         │
├──────────────────────────┤
│ Tags                    │
│ Environment:            │
│ ☑ production            │
│ ☐ staging               │
│ ☐ development           │
│                         │
│ [Reset Filters]         │
└──────────────────────────┘
```

---

### 4. タイムラインスライダー（下部）

```
┌────────────────────────────────────────────────────────────────┐
│ Timeline                                                        │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│ 12:00     14:00     16:00     18:00     20:00     22:00       │
│ ──────────●─────────────────────────────────────────────────> │
│           ↑                                                    │
│      Current Time                                              │
│      2024-12-10 14:30                                          │
│                                                                │
│ Events:                                                        │
│ 12:15 🔴 RDS instance created                                 │
│ 14:20 🟡 Security group modified                              │
│ 14:25 🟡 EC2 CPU spike detected                               │
│                                                                │
│ [Play] [◀] [▶] Speed: 1x                                     │
└────────────────────────────────────────────────────────────────┘
```

**機能：**
- スライダーを動かすと過去のグラフ状態を表示
- イベントをクリックするとその時点にジャンプ
- Play ボタンで自動再生（変化をアニメーション表示）

---

## 実際の技術スタックでの実装例

### 使用ライブラリ

#### Cytoscape.js を使った場合

```javascript
// グラフ初期化
const cy = cytoscape({
  container: document.getElementById('graph-canvas'),

  elements: [
    // ノード
    { data: { id: 'vpc-1', label: 'VPC-production', type: 'vpc', health: 'healthy' } },
    { data: { id: 'subnet-1', label: 'Subnet-1a', type: 'subnet' } },
    { data: { id: 'ec2-1', label: 'web-server-1', type: 'ec2', health: 'warning' } },

    // エッジ
    { data: { source: 'vpc-1', target: 'subnet-1', type: 'ownership' } },
    { data: { source: 'subnet-1', target: 'ec2-1', type: 'network' } }
  ],

  style: [
    {
      selector: 'node[type="vpc"]',
      style: {
        'background-color': '#1976d2',
        'label': 'data(label)',
        'width': 80,
        'height': 80
      }
    },
    {
      selector: 'node[health="warning"]',
      style: {
        'border-color': '#ffa726',
        'border-width': 4
      }
    },
    {
      selector: 'edge[type="ownership"]',
      style: {
        'line-style': 'solid',
        'target-arrow-shape': 'triangle'
      }
    }
  ],

  layout: {
    name: 'dagre',  // 階層レイアウト
    rankDir: 'TB'   // Top to Bottom
  }
});

// ノードクリック時
cy.on('tap', 'node', function(evt){
  const node = evt.target;
  showInspectorPanel(node.data());
});
```

---

## グラフのインタラクション

### 1. ズーム＆パン

```
マウスホイール → ズームイン/アウト
ドラッグ → パン（移動）
ダブルクリック → ノードを中心に表示
```

### 2. ノード操作

```
クリック → インスペクターパネルに詳細表示
ホバー → ツールチップ表示
右クリック → コンテキストメニュー
  - View in AWS Console
  - View Metrics
  - View Traces
  - Inspect Drift
```

### 3. グラフレイアウト

**選択可能なレイアウト：**
- **Hierarchical（階層型）** - VPC → Subnet → EC2 のような階層
- **Force-Directed（力学モデル）** - ノードが自然に配置
- **Circle（円形）** - リソースを円状に配置
- **Grid（格子）** - 整列表示

---

## 実際の表示例（ASCII アート版）

### 小規模構成（1 VPC, 2 Subnet, 3 EC2, 1 RDS）

```
                    ┌─────────────┐
                    │     VPC     │
                    │  production │
                    │    🟦       │
                    └──────┬──────┘
                           │
              ┌────────────┴────────────┐
              │                         │
       ┌──────▼──────┐           ┌─────▼──────┐
       │   Subnet    │           │   Subnet   │
       │  us-east-1a │           │ us-east-1b │
       │     🟨      │           │     🟨     │
       └──────┬──────┘           └─────┬──────┘
              │                        │
       ┌──────┴──────┐          ┌──────┴──────┐
       │             │          │
┌──────▼──────┐┌─────▼─────┐┌──▼─────────┐
│    EC2-1    ││   EC2-2   ││   EC2-3    │
│ web-server  ││ api-server││ worker     │
│    🟩       ││    🟡     ││    🟩      │
│  Healthy    ││  Warning  ││  Healthy   │
└──────┬──────┘└─────┬─────┘└─────┬──────┘
       │             │             │
       └─────────────┴─────────────┘
                     │
              ┌──────▼──────┐
              │     RDS     │
              │  postgres   │
              │     🟧      │
              │   Healthy   │
              └─────────────┘
```

### 中規模構成（複数 VPC、多数のリソース）

この場合はズームレベルによって表示が変わります：

**ズームアウト時：**
```
[VPC-prod]──[20 resources]──🟡
[VPC-stg]───[10 resources]──🟩
[VPC-dev]───[5 resources]───🟩
```

**ズームイン時：**
個別のリソースが全て表示される

---

## リアルタイム更新

### 変化のアニメーション

```
Before (t=0):          After (t=1):

┌─────┐               ┌─────┐
│EC2-1│               │EC2-1│ ← 点滅
│ 🟩  │    →          │ 🟡  │ ← 色変化
└─────┘               └─────┘

                      + 通知バナー:
                      "⚠️ EC2-1 CPU usage high"
```

### WebSocket 更新

```javascript
// WebSocket で変更を受信
ws.onmessage = (event) => {
  const update = JSON.parse(event.data);

  if (update.type === 'drift_detected') {
    // ノードを赤くハイライト
    cy.$(`#${update.resource_id}`).addClass('drift-detected');

    // アニメーション
    cy.$(`#${update.resource_id}`).animate({
      style: { 'border-width': 8 }
    }, {
      duration: 500
    });
  }
};
```

---

## まとめ

SkyGraph の UI は：

✅ **直感的** - ドラッグ＆ドロップで操作
✅ **リアルタイム** - 変更を即座に反映
✅ **インタラクティブ** - クリック・ホバーで詳細表示
✅ **カラフル** - 状態が一目でわかる
✅ **スケーラブル** - 10〜10,000 リソースまで対応

次のステップとして、実際の UI モックアップ（HTML + Cytoscape.js）を作成しますか？
